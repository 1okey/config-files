# todo:
# - install needed dependencies for certain platforms
# --- install zsh + ohmyzsh
# --- install curl, git etc.
# - create symlinks or rather copy config files and `source` them
# - --update flag should update already existing symlinks (of copies) of config files
